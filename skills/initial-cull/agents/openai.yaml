interface:
  display_name: "初筛"
  short_description: "按审核清单整理、分组、评星，并可写入 RAW 直方图驱动的 LR 粗修 XMP。"
  default_prompt: "Use $initial-cull to organize this photo directory, create and review a complete ratings manifest, apply ratings safely, optionally apply a reviewed Lightroom plan, and verify the result."

policy:
  allow_implicit_invocation: true
