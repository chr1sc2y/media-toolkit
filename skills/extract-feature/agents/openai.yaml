interface:
  display_name: "成片归档"
  short_description: "精修完成后按 Lightroom Export 清单复制匹配 HIF 到显式目标目录。"
  default_prompt: "Use $extract-feature to finalize this refined photo directory; ask for --copy-to if no destination is provided."

policy:
  allow_implicit_invocation: true
